{% extends 'base.html.twig' %}

{% block title %}Gerer les categories{% endblock %}

{% block body %}
<div class="container-fluid">
	<div class="row">
		<div class="left ">
        <ul>
            <a href="{{ path('admin_home') }}">
                <li class="item-menu">
                    <i class="fa fa-home fa-2x" aria-hidden="true"></i> 
                    <span class="menu">Admin</span>
                </li>
            </a> 
            <a href="ajout">
                <li class="item-menu">
                    <i class="fas fa-tag fa-2x" aria-hidden="true"></i>
                    <span class="menu">Ajouter une catégorie</span>
                </li>
            </a>    
            
        </ul>
    </div> <!-- end left -->
    <div class="container ">
    <div class="content">
    
        <h1>Gerer les categories</h1>
        <table class="table table-striped">
  <thead>
    <tr>
      <th scope="col">Id</th>
      <th scope="col">Nom</th>
      <th scope="col">Parent</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
    {% for categorie in categories %}
    <tr>
      <th scope="row">{{categorie.id}}</th>
      <td>{{categorie.name}}</td>
      <td>{{categorie.parent}}</td>
      <td class="text-center">
      <a class="btn btn-primary" href="{{ path("admin_categories_modifier", {id: categorie.id}) }}" role="button">modifier</a>
      </td>
    </tr>
    {% endfor %}
  </tbody>
</table>
    </div>
     </div>
    </div>
	</div>
</div>
{% endblock %}
